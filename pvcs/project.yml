apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-volume1-claim
  namespace: project
spec:
  storageClassName: local-volume1-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
