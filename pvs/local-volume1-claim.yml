apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-volume1-claim
spec:
  storageClassName: local-volume1-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
